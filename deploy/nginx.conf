server {
    listen 80;
    server_name lead.prav-buro.ru;

    location /bitrix/webhook {
        proxy_pass http://127.0.0.1:8080/bitrix/webhook;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
